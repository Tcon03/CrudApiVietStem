<Window x:Class="testInterFace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:testInterFace"
        mc:Ignorable="d"
         WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" FontFamily="Inter"
       Title="Edit Contest Form" Height="50" Width="800">
    <!--<Grid>
        <Border Grid.Row="1"
                Background="#634BD4 ">
            <StackPanel Orientation="Vertical" 
                      Margin="0 40">
                <Button 
                      Style="{StaticResource btnSideBar}">

                    <StackPanel Orientation="Horizontal">
                       
                        <TextBlock Text="Contest" 
                                 VerticalAlignment="Center"
                                 FontSize="25"
                                 Margin="5"/>
                    </StackPanel>

                </Button>
                <Button  Style="{StaticResource btnSideBar}"
                        >
                    <StackPanel Orientation="Horizontal">
                   
                        <TextBlock Text="City"
                                  VerticalAlignment="Center"
                                   FontSize="25"
                                   Margin="10 0"/>

                    </StackPanel>
                </Button>
            </StackPanel>

        </Border>
    </Grid>-->

    <Grid>
        <DatePicker 
            Grid.Column="1"
            FontSize="15"
            VerticalContentAlignment="Center"
            Margin="10 0 10 0"
            Height="40"
            BorderBrush="Gray"
            BorderThickness="1">
            <DatePicker.Resources>
                <Style TargetType="{x:Type DatePickerTextBox}">
                    <Setter Property="Control.Template">
                        <Setter.Value>
                            <ControlTemplate>
                                <Border BorderBrush="Gray" 
                                BorderThickness="1" 
                                CornerRadius="5"
                                Background="White">
                                    <TextBox x:Name="PART_TextBox" 
                                     BorderThickness="0"
                                     Background="Transparent"
                                     VerticalContentAlignment="Center"
                                     Padding="10,0"
                                     Text="{Binding Path=SelectedDate, RelativeSource={RelativeSource AncestorType={x:Type DatePicker}}}" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DatePicker.Resources>
        </DatePicker>
    </Grid>
</Window>
