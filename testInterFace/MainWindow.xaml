<Window x:Class="testInterFace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:testInterFace"
        mc:Ignorable="d"
         WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" FontFamily="Inter"
       Title="Edit Contest Form" Height="650" Width="800">
    <!--<Grid>
        <Border Grid.Row="1"
                Background="#634BD4 ">
            <StackPanel Orientation="Vertical" 
                      Margin="0 40">
                <Button 
                      Style="{StaticResource btnSideBar}">

                    <StackPanel Orientation="Horizontal">
                       
                        <TextBlock Text="Contest" 
                                 VerticalAlignment="Center"
                                 FontSize="25"
                                 Margin="5"/>
                    </StackPanel>

                </Button>
                <Button  Style="{StaticResource btnSideBar}"
                        >
                    <StackPanel Orientation="Horizontal">
                   
                        <TextBlock Text="City"
                                  VerticalAlignment="Center"
                                   FontSize="25"
                                   Margin="10 0"/>

                    </StackPanel>
                </Button>
            </StackPanel>

        </Border>
    </Grid>-->

    <Window.Resources>
        <!-- Định nghĩa các màu sắc chính để dễ dàng thay đổi -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#4F46E5"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#4338CA"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#D1D5DB"/>
        <SolidColorBrush x:Key="BackgroundLightBrush" Color="#F9FAFB"/>

        <!-- Style cho TextBlock làm nhãn (Label) -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Style cho TextBox nhập liệu -->
        <Style x:Key="StyledTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                CornerRadius="8" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <!-- Placeholder Text -->
                                <TextBlock Text="{TemplateBinding Tag}" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="14,0,0,0"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed"
                                           x:Name="Placeholder"/>
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hiệu ứng khi focus -->
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" Color="{Binding Color, Source={StaticResource PrimaryBrush}}" Opacity="0.5" BlurRadius="8"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <!-- Hiển thị placeholder khi TextBox trống -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Visibility" TargetName="Placeholder" Value="Visible"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho Nút chính (Primary Button) -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho Nút phụ (Secondary Button) -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" 
                                        Property="Background"
                                        Value="{StaticResource BackgroundLightBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho nút đóng cửa sổ (Close Button) -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="5">
                            <Path x:Name="icon" Data="M0,0 L12,12 M0,12 L12,0" 
                                  Stroke="{TemplateBinding Foreground}"
                                  StrokeThickness="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="icon" Property="Stroke"
                                        Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Thân cửa sổ chính -->
    <Border CornerRadius="12" Padding="1"
            Background="{StaticResource BackgroundLightBrush}">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="30" Color="Black" Opacity="0.15"/>
        </Border.Effect>

        <!-- Container chính của Form -->
        <Border Background="White" CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Header -->
                    <RowDefinition Height="*"/>
                    <!-- Content -->
                    <RowDefinition Height="Auto"/>
                    <!-- Footer -->
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" x:Name="Header"
                        BorderBrush="{StaticResource BorderBrush}" 
                        BorderThickness="0,0,0,1"
                        MouseDown="Header_MouseDown">
                    <Grid Margin="24,20">
                        <TextBlock Text="Chỉnh Sửa Cuộc Thi" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <Button x:Name="BtnClose" Style="{StaticResource CloseButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="BtnClose_Click"/>
                    </Grid>
                </Border>

                <!-- Nội dung Form -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="32">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Cột 1 -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,0">
                                <TextBlock Style="{StaticResource FormLabel}"
                                           Text="Tiêu đề cuộc thi"/>
                                <TextBox Style="{StaticResource StyledTextBox}"
                                         Tag="Ví dụ: Cuộc thi thiết kế logo..."/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,20,12,0">
                                <TextBlock Style="{StaticResource FormLabel}" 
                                           Text="Mô tả chi tiết"/>
                                <TextBox Style="{StaticResource StyledTextBox}" 
                                         Tag="Nhập mô tả về cuộc thi..." TextWrapping="Wrap" AcceptsReturn="True" MinHeight="120" VerticalContentAlignment="Top" Padding="12,10"/>
                            </StackPanel>

                            <!-- Cột 2 -->
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="12,0,0,0">
                                <TextBlock Style="{StaticResource FormLabel}" Text="Từ khóa"/>
                                <TextBox Style="{StaticResource StyledTextBox}" Tag="Ví dụ: thiết kế, sáng tạo..."/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="12,20,0,0">
                                <TextBlock Style="{StaticResource FormLabel}" Text="Thông tin bổ sung 1"/>
                                <TextBox Style="{StaticResource StyledTextBox}" Tag="Ví dụ..."/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="1" Margin="12,20,0,0">
                                <TextBlock Style="{StaticResource FormLabel}" Text="Thông tin bổ sung 2"/>
                                <TextBox Style="{StaticResource StyledTextBox}" Tag="Ví dụ..."/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <!-- Footer với các nút hành động -->
                <Border Grid.Row="2" Background="{StaticResource BackgroundLightBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" CornerRadius="0,0,12,12">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="32,16">
                        <Button Content="Hủy" Style="{StaticResource SecondaryButton}" Margin="0,0,12,0"/>
                        <Button Content="Lưu thay đổi" Style="{StaticResource PrimaryButton}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>
